#include <stdio.h>

int main()
{
    int A = 0, B = 0;
    printf("Enter number for A:");
    scanf("%d", &A);

    printf("Enter number for B:");
    scanf("%d", &B);

    printf("A:%d B:%d", A, B);

    return 0;
}